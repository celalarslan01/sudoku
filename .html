<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    #sudoku-container {
      text-align: center;
    }

    #sudoku-board {
      border-collapse: collapse;
      margin-top: 20px;
    }

    #sudoku-board td {
      width: 30px;
      height: 30px;
      border: 1px solid #000;
    }

    button {
      margin-top: 10px;
      padding: 10px;
      font-size: 16px;
      cursor: pointer;
    }
  </style>
  <title>Sudoku Çözücü</title>
</head>
<body>
  <div id="sudoku-container">
    <table id="sudoku-board"></table>
    <button onclick="solveSudoku()">Çöz</button>
  </div>
  <script>
    const sudokuBoard = [
      [5, 3, 0, 0, 7, 0, 0, 0, 0],
      [6, 0, 0, 1, 9, 5, 0, 0, 0],
      [0, 9, 8, 0, 0, 0, 0, 6, 0],
      [8, 0, 0, 0, 6, 0, 0, 0, 3],
      [4, 0, 0, 8, 0, 3, 0, 0, 1],
      [7, 0, 0, 0, 2, 0, 0, 0, 6],
      [0, 6, 0, 0, 0, 0, 2, 8, 0],
      [0, 0, 0, 4, 1, 9, 0, 3, 5],
      [0, 0, 0, 0, 8, 0, 0, 7, 9]
    ];

    function solveSudoku() {
      // Sudoku çözme algoritmasını buraya ekleyin
      // Örneğin: sudokuSolver(sudokuBoard);
    }

    function renderBoard(board) {
      const table = document.getElementById("sudoku-board");
      table.innerHTML = ""; // Tabloyu temizle

      for (let i = 0; i < 9; i++) {
        const row = document.createElement("tr");
        for (let j = 0; j < 9; j++) {
          const cell = document.createElement("td");
          cell.textContent = board[i][j] === 0 ? "" : board[i][j];
          row.appendChild(cell);
        }
        table.appendChild(row);
      }
    }

    // Başlangıçta sudoku tahtasını ekrana render et
    renderBoard(sudokuBoard);
  </script>
</body>
</html>
